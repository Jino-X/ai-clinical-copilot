# Runtime dependencies. Pinned exactly — this service handles PHI and an
# unreviewed transitive upgrade is not an acceptable deployment surprise.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
httpx==0.28.1
asyncpg==0.31.0
structlog==26.1.0
pyjwt==2.13.0
cryptography==50.0.0
# Backs pydantic's EmailStr. Email is how a member is invited, so a typo must
# fail validation rather than create an unreachable membership.
email-validator==2.3.0

python-docx==1.2.0
pypdf==6.16.2
